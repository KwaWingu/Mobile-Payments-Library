apply from: rootProject.file('gradle/plugin-java.gradle')

dependencies {
    // api: these appear in the impl's public method signatures (payload + response types), so
    // consumers need them on their compile classpath.
    api project(':kw-mpesa-payment-api')
    api project(':kw-mobile-payment-client')
    testImplementation libs.slf4j
}

apply from: rootProject.file('gradle/plugin-publish.gradle')
